'use strict';

/* Loaded through require() by src/main/plugins/keychain/keychain.ts, which treats a throw here as
   "no keychain" — an unbuilt or non-macOS checkout must not stop the app from starting. */
module.exports = require('./build/Release/keychain.node');
