'use strict';

/* Loaded through require() by src/main/vault/secure-enclave-key.ts, which treats a throw here as
   "no hardware" — an unbuilt or non-macOS checkout must not stop the app from starting. */
module.exports = require('./build/Release/hardware_key.node');
