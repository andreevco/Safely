import { Title, Group, Footer } from './components';
import { ListContainer } from './List';

export const List = Object.assign(ListContainer, {
    Title: Title,
    Group: Group,
    Footer: Footer
});
