import { Content, Header, List, Scrollable } from './components';
import { ScreenContainer } from './Screen';

export const Screen = Object.assign(ScreenContainer, { Content, Header, Scrollable, List });
