import { Column, Label, Value } from './components';
import { TableCellContainer } from './TableCell';

export const TableCell = Object.assign(TableCellContainer, {
    Column,
    Label,
    Value
});
