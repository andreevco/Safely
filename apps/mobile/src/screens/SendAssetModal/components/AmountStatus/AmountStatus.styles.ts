import { StyleSheet } from 'react-native-unistyles';

export const styles = StyleSheet.create(() => ({
    row: {
        flexDirection: 'row',
        alignItems: 'center'
    },
    infoIcon: {
        padding: 4
    }
}));
