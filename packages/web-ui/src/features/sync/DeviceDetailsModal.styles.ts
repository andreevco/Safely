import { css } from '@safely/web-ui/styled-system/css';

export const popupStyles = css({ width: '480px' });

export const listsStyles = css({
    display: 'flex',
    flexDirection: 'column',
    gap: '8',
    padding: '8',
    paddingBottom: '16'
});

export const bannerStyles = css({ marginTop: '8' });

export const statusColumnStyles = css({
    display: 'flex',
    flexDirection: 'column',
    gap: '2'
});

export const walletsStyles = css({
    display: 'flex',
    flexDirection: 'column',
    gap: '2',
    minWidth: '0'
});
