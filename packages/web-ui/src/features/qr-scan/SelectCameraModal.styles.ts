import { css } from '@safely/web-ui/styled-system/css';

export const listStyles = css({ paddingInline: '8', paddingBottom: '8' });

export const actionsStyles = css({ display: 'flex', padding: '8' });
