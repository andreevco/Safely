import { css } from '@safely/web-ui/styled-system/css';

export const listStyles = css({ padding: '8', overflowY: 'auto' });

export const addressBookStyles = css({ paddingInline: '8', paddingTop: '8' });
