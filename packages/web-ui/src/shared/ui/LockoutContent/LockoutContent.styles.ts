import { css } from '@safely/web-ui/styled-system/css';

export const lockoutStyles = css({
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
    gap: '4',
    textAlign: 'center'
});
