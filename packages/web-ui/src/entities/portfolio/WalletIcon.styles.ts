import { css } from '@safely/web-ui/styled-system/css';

export const emojiStyles = css({
    fontSize: '16px',
    lineHeight: '20px'
});

export const emojiMediumStyles = css({
    fontSize: '20px',
    lineHeight: '24px'
});

export const colorStyles = css({
    display: 'block',
    width: '16px',
    height: '16px',
    borderRadius: 'full',
    backgroundColor: 'var(--wallet-color)'
});

export const colorMediumStyles = css({
    display: 'block',
    width: '20px',
    height: '20px',
    borderRadius: 'full',
    backgroundColor: 'var(--wallet-color)'
});
