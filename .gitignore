.idea
node_modules
build
dist
# panda codegen output — regenerated by `panda codegen`, never committed
styled-system
# electron-forge / vite build output
.vite
out
# panda codegen output — regenerated by `panda codegen`, never committed
styled-system
# electron-forge / vite build output
.vite
out
.env
# signing inputs: the profile names the machines allowed to run the build, the .p12 holds a key
*.provisionprofile
*.p12
tsconfig.tsbuildinfo
.DS_Store
CLAUDE.local.md
.claude/local/
.claude/agents/local/
.claude/settings.local.json
.claude/.cc-writes
.pnpm-store
