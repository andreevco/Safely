{
    "readme": "Reviewed exceptions to the JavaScript dependency advisory gate. See .claude/rules/dependency-security.md before editing. Checked by scripts/audit-gate.mjs.",
    "policy": {
        "blockingSeverities": [
            "critical",
            "high"
        ]
    },
    "exceptions": [
        {
            "id": "GHSA-5p2g-fcmc-qvqq",
            "package": "image-size",
            "severity": "high",
            "context": "rn-dev",
            "paths": [
                "apps/mobile > expo > @expo/metro > metro > image-size",
                "apps/mobile > react-native > @react-native/community-cli-plugin > metro > image-size"
            ],
            "rationale": "image-size is Metro's asset-dimension reader: it parses image headers while the bundler resolves assets, and behind the dev-server asset endpoint under `expo start`. It reaches the production graph only because Metro is a transitive dependency of react-native and expo — no Metro code is in a release bundle, which carries the computed dimensions rather than the parser. The images it parses are the assets committed to this repository; the dev-server path exists on a developer machine only and is not present in any shipped build. Metro declares `image-size: ^1.0.2` and resolves 1.2.1, the newest release of the 1.x line (npm's `legacy` tag). The advisory names `>=2.0.3` as patched and npm has no such version — `latest` is 2.0.2, itself inside the vulnerable range — so no `overrides` entry can close this; the only fix on offer would be forcing Metro across a major.",
            "owner": "sergey@andreevco.com",
            "expires": "2026-11-30",
            "url": "https://github.com/advisories/GHSA-5p2g-fcmc-qvqq"
        },
        {
            "id": "GHSA-w3rx-r6r6-pgpr",
            "package": "image-size",
            "severity": "high",
            "context": "rn-dev",
            "paths": [
                "apps/mobile > expo > @expo/metro > metro > image-size",
                "apps/mobile > react-native > @react-native/community-cli-plugin > metro > image-size"
            ],
            "rationale": "Same package and same reachability as GHSA-5p2g-fcmc-qvqq: Metro's bundle-time and dev-server image-header parser, reachable through react-native and expo but absent from every release bundle, and fed only by assets committed to this repository. Metro declares `image-size: ^1.0.2` and resolves 1.2.1, the newest release of the 1.x line (npm's `legacy` tag). The advisory names `>=2.0.3` as patched and npm has no such version — `latest` is 2.0.2, itself inside the vulnerable range — so no `overrides` entry can close this; the only fix on offer would be forcing Metro across a major.",
            "owner": "sergey@andreevco.com",
            "expires": "2026-11-30",
            "url": "https://github.com/advisories/GHSA-w3rx-r6r6-pgpr"
        }
    ]
}
